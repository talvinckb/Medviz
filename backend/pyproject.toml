[project]
name = "backend"
version = "0.1.0"
description = "Backend de notre super projet medviz"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "httpx2>=2.2.0",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest-cov>=7.1.0",
]
